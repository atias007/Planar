$kind: http-request
url: http://localhost:2306/config/PostmanTestConfig
method: DELETE
auth:
  type: noauth
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status code is 404 (key already deleted)", function () {
          pm.response.to.have.status(404);
      });
    language: text/javascript
order: 12000
