$kind: http-request
url: http://localhost:2306/service/security-audits?pageNumber=1&pageSize=0
method: GET
queryParams:
  pageNumber: "1"
  pageSize: "0"
auth:
  type: noauth
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status code is 400", function () {
          pm.response.to.have.status(400);
      });
    language: text/javascript
order: 11000
