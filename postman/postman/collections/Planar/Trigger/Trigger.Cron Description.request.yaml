$kind: http-request
url: http://localhost:2306/trigger/cron?expression=0 0/5 * * * ?
method: GET
queryParams:
  expression: 0 0/5 * * * ?
auth:
  type: noauth
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status code is 200", function () {
          pm.response.to.have.status(200);
      });

      pm.test("Has cron description text", function () {
          var text = pm.response.text();
          pm.expect(text).to.not.be.empty;
          pm.expect(text.toLowerCase()).to.include("5 minutes");
      });
    language: text/javascript
order: 17000
