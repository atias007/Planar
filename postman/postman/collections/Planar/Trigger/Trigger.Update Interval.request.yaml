$kind: http-request
url: http://localhost:2306/trigger/interval
method: PATCH
body:
  type: json
  content: |-
    {
        "id": "{{triggerId}}",
        "interval": "00:12:00"
    }
auth:
  type: noauth
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status code is 204", function () {
          pm.response.to.have.status(204);
      });
    language: text/javascript
order: 5000
